/*
function solicita(n : number ) : void {
   if(n >= 0) console.log(`El factorial del numero ${n} es ${factorial(n)}`);
   else console.log(`El cubo del numero ${n} es ${alCubo(n)}`)
}

solicita(3);
solicita(5);
solicita(-1);
solicita(-5); */
